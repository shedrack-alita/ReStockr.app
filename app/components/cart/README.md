# components/cart

`CartDrawer.vue`, `CartItem.vue`, `CartSummary.vue`. Not built yet — a
previous cart implementation (localStorage-only, no server revalidation)
was removed along with the mock backend; this will be rebuilt against the
real backend API once the product catalog exists to add items from.
