# components/account

`ProfileCard.vue`, `AddressForm.vue`, `OrderHistory.vue`. Not built yet —
depends on the auth feature (in progress) being wired to a real backend,
since there's no session to show a profile for until then.
