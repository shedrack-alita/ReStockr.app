# pages/account

Signed-in account area (profile, addresses, settings). Protected by
`middleware/auth.ts` once built. Not built yet — the auth feature (login/
register/password-reset) ships first; account pages come after there's a
real session to protect.
