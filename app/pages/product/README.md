# pages/product

`[slug].vue` — product detail page. Not built yet — depends on the product
catalog feature (real backend API `server/api/products/*`, still a
placeholder — see server/api/products/README.md).
