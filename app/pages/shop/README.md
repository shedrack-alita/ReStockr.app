# pages/shop

Product listing/shop page(s) (e.g. `index.vue`, filtered/paginated views).
Not built yet — depends on the product catalog feature. `.vue` files here
become real routes the moment they're added; this README is just a
placeholder so the folder isn't a mystery empty directory.
