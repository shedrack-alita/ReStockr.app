/** Order history state — not built yet. Depends on checkout existing to create orders from. */
export const useOrdersStore = defineStore('orders', () => {
  // TODO: items, fetch(), fetchOne(id).
  return {}
})
