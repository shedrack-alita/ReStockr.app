/** Wishlist state — not built yet. Depends on the product catalog existing. */
export const useWishlistStore = defineStore('wishlist', () => {
  // TODO: items, has(productId), toggle(productId).
  return {}
})
