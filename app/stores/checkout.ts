/**
 * Checkout flow state (shipping address, payment method, order review) —
 * not built yet. Depends on the cart feature + a real payment provider
 * decision, neither of which exist yet.
 */
export const useCheckoutStore = defineStore('checkout', () => {
  // TODO: shippingAddress, paymentMethod, placeOrder().
  return {}
})
