/**
 * Product catalog state (listing/filter/sort) — not built yet. Depends on
 * server/api/products/* (still a placeholder, see its README) being
 * implemented against a real backend.
 */
export const useProductsStore = defineStore('products', () => {
  // TODO: items, filters, pagination, fetch().
  return {}
})
